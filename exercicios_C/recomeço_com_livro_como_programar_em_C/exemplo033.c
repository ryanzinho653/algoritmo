/* Repeticao controlada por contador for*/
#include <stdio.h>

int main(void)
{
    int contador;

    for (contador = 1; contador <= 10; contador++)
    {
        printf("%d\n", contador);
    }

    return 0;
}